/*
 * MFOC 2026 Badge Base Station — Arduino IDE entry point
 *
 * Open this file in Arduino IDE. The IDE automatically compiles the shared
 * implementation in src/main.cpp and its headers, so PlatformIO and Arduino
 * IDE use the same firmware source.
 *
 * Before compiling, install "IRremote" by Armin Joachimsmeyer from Arduino
 * IDE's Library Manager. Pin assignments, station number, and operating mode
 * are configured in src/config.h.
 */
